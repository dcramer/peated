{
  "$schema": "https://turborepo.org/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        ".next/**",
        "!.next/cache/**",
        "build/**",
        "node_modules/.cache/metro/**",
        "dist/**"
      ]
    },
    "mobile#build": {
      "env": ["API_SERVER"]
    },
    "web#build": {
      "env": ["API_SERVER", "FATHOM_SITE_ID", "SESSION_SECRET"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "cli#dev": {
      "passThroughEnv": ["DATABASE_URL", "JWT_SECRET"]
    },
    "server#dev": {
      "passThroughEnv": ["CORS_HOST", "DATABASE_URL", "JWT_SECRET"]
    },
    "web#dev": {
      "passThroughEnv": ["SESSION_SECRET"]
    },
    "test": {
      "dependsOn": ["build"],
      "inputs": ["**/*.{ts,tsx,js,jsx}"],
      "passThroughEnv": ["DATABASE_URL", "JWT_SECRET", "SESSION_SECRET"]
    },
    "typecheck": {}
  },
  "globalEnv": [
    "DEBUG",
    "HOST",
    "PORT",
    "API_SERVER",
    "URL_PREFIX",
    "VERSION",
    "GOOGLE_CLIENT_ID",
    "SENTRY_DSN"
  ],
  "globalPassThroughEnv": [
    "NODE_ENV",
    "CI",
    "VERCEL",
    "VERCEL_ENV",
    "VERCEL_URL",
    "npm_lifecycle_event"
  ]
}
