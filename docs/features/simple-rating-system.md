# Simple Rating System

This document has been consolidated into
[Rating Systems Architecture](../architecture/rating-systems.md), which owns the
current Pass/Sip/Savor product behavior, persistence, aggregates, accessibility,
and historical migration contract.

Keep this pointer until historical change inventories no longer reference the
old path; new documentation must link to the architecture document directly.
