import { LoadingRecordList } from "@peated/web/components/designSystem/components";

export default function EntityBottlesLoading() {
  return <LoadingRecordList label="Loading entity bottles" rows={4} />;
}
