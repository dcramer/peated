// The persistent inbox layout owns all visible loading feedback. This boundary
// covers only the route marker used to validate the selected task URL.
export default function InboxSelectionLoading() {
  return null;
}
