{
  "private": true,
  "name": "cask",
  "version": "0.1.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "run-p start:*",
    "start:web": "cd cask-web && API_SERVER=http://localhost:4000 dotenv -e ../.env -- npm start",
    "start:server": "cd cask-server && PORT=4000 dotenv -e ../.env -- npm start",
    "db:studio": "cd cask-server && dotenv -e ../.env -- npx prisma studio",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "dotenv": "^16.0.3",
    "dotenv-cli": "^7.1.0",
    "npm-run-all": "^4.1.5"
  }
}
