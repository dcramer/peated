{
  "private": true,
  "name": "@peated/shared",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest --single-thread",
    "test:ci": "vitest --no-threads --coverage"
  },
  "files": [
    "lib",
    "schemas.ts",
    "types.ts",
    "constants.ts",
    "tsconfig.json",
    "tsconfig.node.json"
  ],
  "dependencies": {
    "@peated/tsconfig": "workspace:*",
    "@vitest/coverage-v8": "^0.34.5",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.2.2",
    "vite-tsconfig-paths": "^4.2.1",
    "vitest": "^0.34.5",
    "zod": "^3.22.2"
  }
}
