{
  "private": true,
  "name": "@peated/shared",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest --single-thread",
    "test:ci": "vitest --no-threads --coverage --reporter=junit --outputFile=junit.xml --reporter=default"
  },
  "files": [
    "lib",
    "schemas.ts",
    "types.ts",
    "constants.ts",
    "tsconfig.json",
    "tsconfig.node.json"
  ],
  "dependencies": {
    "@peated/tsconfig": "workspace:*",
    "@vitest/coverage-v8": "^0.34.1",
    "typescript": "^5.1.6",
    "vite-tsconfig-paths": "^4.2.0",
    "vitest": "^0.34.1",
    "zod": "^3.22.1"
  }
}
