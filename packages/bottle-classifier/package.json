{
  "private": true,
  "name": "@peated/bottle-classifier",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "evals": "dotenv -e ../../.env -e ../../.env.local -- vitest run --config vitest.evals.config.mts",
    "test": "vitest run --config vitest.config.mts",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false"
  },
  "dependencies": {
    "@openai/agents": "^0.6.0",
    "@peated/tsconfig": "workspace:*",
    "openai": "^6.27.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tinyrainbow": "^2.0.0",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "vitest-evals": "^0.7.0"
  },
  "volta": {
    "extends": "../../package.json"
  }
}
