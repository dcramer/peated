# Value Audit (tasks.md 1.7)

Every classifier component (output field, input field, tool, runtime stage) must
have an identifiable **consumer** or a **measured decision effect**, per the
requirement "Agent components must earn their place" (`spec.md:194-211`).

Verdicts:

- **KEEP** — a real consumer is cited (decision-affecting code, or a review/display
  surface the product depends on).
- **REMOVE** — no consumer and no plausible decision value; the model fills it and
  nothing reads it.
- **ABLATE** — plausible value but unproven; the one-line experiment to prove it is
  given. Includes fields the spec mandates but that no code/eval reads _today_.

Consumer classes used below: **decision-affecting** (changes an action/target/tier),
**display-review** (rendered to a human or persisted for triage), **telemetry-only**
(Sentry/log/metric write), **eval-scorer-only**, **NO CONSUMER** (write-only).

Builds on: `output-contract-map.md` (confidence/band consumer inventory),
`input-envelope-and-terminology.md` (envelope REMOVE/EXPLAIN verdicts),
`contradiction-inventory.md`. Not re-derived here.

---

## A. Output fields — `BottleClassifierAgentDecisionSchema` (`classifierTypes.ts:638-678`)

### A.1 Top-level fields

| Field                | Consumer(s) with file:line                                                                                                                                                                                                                                 | Class                                                            | Verdict                                                                                                                                                                                                                                                                |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `action`             | `reviewPolicy.ts:2916/3226/3319/3693` (action-branched finalize), `priceMatchingProposals.ts:2079`, `photo-identification.ts:83`, `bottleReferenceResolution.ts:82`                                                                                        | decision-affecting                                               | **KEEP**                                                                                                                                                                                                                                                               |
| `confidence`         | 18 decision sites inventoried in `output-contract-map.md:116-142` (`priceMatchingEvidence.ts:38-40,90-137`, `reviewPolicy.ts:2609/2634/2658/2887`, `photo-identification.ts:68,83`, `classifierRuntime.ts:198`, `bottleReferenceResolution.ts:411`)        | decision-affecting                                               | **REMOVE** — per `design.md:117-131`; migrate all 18 to derived tier. (Established; not re-derived.)                                                                                                                                                                   |
| `rationale`          | `priceMatchingProposals.ts:572-726,1629,2494` (stored on proposal rows), `photo-identification.ts:254,318` (Sentry `reason`), `admin/incoming-bottle-decisions.ts:173`, `matchQueue/utils.ts:377`, `queue/llmExport.ts:171`, `createMissingBottles.ts:133` | display-review + telemetry only — **no decision-affecting read** | **KEEP (demote)** — survives as freeform review/telemetry text; must not gate. Matches `output-contract-map.md:38`.                                                                                                                                                    |
| `candidateBottleIds` | `reviewPolicy.ts:2916/3226/3319/3693` (`candidateBottleIds.has(matchedBottleId/parentBottleId)` — rejects out-of-candidate targets), `bottleReferenceResolution.ts:82,101` (same guard), `legacyReleaseRepairResolution.ts:88-89`, `smwsPolicy.ts:395-396` | decision-affecting                                               | **KEEP** — this resolves the `output-contract-map.md:39` "needs semantics or removal" open question: it _is_ consumed as an anti-hallucination guard on target ids. Needs semantics in the prompt, not removal.                                                        |
| `identityScope`      | `priceMatchingEvidence.ts:133-134` (exact_cask threshold), `classifierTypes.ts:594-615` superRefine, eval scorer `classifier.eval.test.ts:533/556/585`, `reviewPolicy.ts:1985` exact-cask anchor path                                                      | decision-affecting + eval                                        | **KEEP**                                                                                                                                                                                                                                                               |
| `aliasScope`         | `priceMatchingProposals.ts:577-729` (persisted as `aliasScope ?? "none"`), eval scorer `classifier.eval.test.ts:380-384`. **No code reads the stored value to gate `global_alias` alias creation** (grep for stored-`aliasScope` reads returns none).      | display-persist + eval-scorer only — **no live gating consumer** | **ABLATE** — spec-mandated (`spec.md:122-139`) but the write-gating consumer it exists for is not implemented. Experiment: wire alias creation to read it, then measure global-alias precision with vs without the gate; if no wiring lands, it is a REMOVE candidate. |
| `observation`        | subfields split in A.3                                                                                                                                                                                                                                     | —                                                                | **KEEP (trim subfields)**                                                                                                                                                                                                                                              |
| `identityBasis`      | subfields split in A.2                                                                                                                                                                                                                                     | —                                                                | see A.2                                                                                                                                                                                                                                                                |
| `confidenceBasis`    | subfields split in A.4                                                                                                                                                                                                                                     | —                                                                | see A.4                                                                                                                                                                                                                                                                |
| `matchedBottleId`    | `reviewPolicy.ts:2916`, `bottleReferenceResolution.ts:82`, `priceMatchingProposals.ts`, `photo-identification.ts`                                                                                                                                          | decision-affecting                                               | **KEEP**                                                                                                                                                                                                                                                               |
| `matchedReleaseId`   | `reviewPolicy.ts` finalize, `priceMatchingEvidence.ts:129`, superRefine `classifierTypes.ts:608`                                                                                                                                                           | decision-affecting                                               | **KEEP**                                                                                                                                                                                                                                                               |
| `parentBottleId`     | `reviewPolicy.ts:3226/3319`, `bottleReferenceResolution.ts:101`                                                                                                                                                                                            | decision-affecting                                               | **KEEP**                                                                                                                                                                                                                                                               |
| `proposedBottle`     | `reviewPolicy.ts:2831` (`sanitizeProposedBottleDraft`), `priceMatchingProposals.ts`, `createMissingBottles.ts`                                                                                                                                             | decision-affecting                                               | **KEEP**                                                                                                                                                                                                                                                               |
| `proposedRelease`    | `reviewPolicy.ts` finalize, `priceMatchingProposals.ts`                                                                                                                                                                                                    | decision-affecting                                               | **KEEP**                                                                                                                                                                                                                                                               |

### A.2 `identityBasis.*` (`classifierTypes.ts:410-451`) — the strongest removal cluster

`reviewPolicy.ts` only ever **passes `identityBasis` through unread**
(`:2451, :3805, :3914, :3955`); a targeted grep for reads of any nested field
(`.bottleTraits`, `.releaseTraits`, `.yearInterpretation`, `.siblingEvidence`,
`.uncertainties`) inside `reviewPolicy.ts` returns **zero**. The eval scorers read
`identityScope`, `aliasScope`, and `confidenceBand` but **no `identityBasis` field**
(`classifier.eval.test.ts`, `evalFixtureSchemas.ts:127-139`). The only writers are
the deterministic fast-paths (`service.ts:261-268`, `smwsPolicy.ts:470-479`).

| Nested field         | Consumer                                                              | Class       | Verdict                                                            |
| -------------------- | --------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------ |
| `bottleTraits`       | writers only (`service.ts:262`, `smwsPolicy.ts:473`)                  | NO CONSUMER | **ABLATE** (mandated; write-only today)                            |
| `releaseTraits`      | writers only (`service.ts:263`, `smwsPolicy.ts:474`)                  | NO CONSUMER | **ABLATE**                                                         |
| `observationTraits`  | writers only (`service.ts:265`, `smwsPolicy.ts:475`)                  | NO CONSUMER | **ABLATE**                                                         |
| `yearInterpretation` | writers only, always `"none"` (`service.ts:266`, `smwsPolicy.ts:478`) | NO CONSUMER | **ABLATE**                                                         |
| `siblingEvidence`    | writers only, always `"none"` (`service.ts:267`, `smwsPolicy.ts:479`) | NO CONSUMER | **ABLATE**                                                         |
| `uncertainties`      | **no reference anywhere** (grep returns only the schema def)          | NO CONSUMER | **REMOVE** — not even written by the fast-paths; pure dead weight. |

Whole-object note: `identityBasis` is spec-required (`spec.md:146-153`) as the
structure code will derive gating from, but **today nothing consumes it and no eval
scores it**. It is currently unread weight the model must fill on every run. The
`design.md` typed-trait migration (`output-contract-map.md:254-297`) is what would
give it a mechanical consumer.

**Ablation for the whole object:** land the review-policy placement check that reads
`releaseTraits`/`bottleTraits` (a trait in `releaseTraits` must not appear in
`proposedBottle.name`) and re-run focused create/repair evals; keep the sub-fields
only if the check catches real mis-placements. Until a reader exists, everything
except `uncertainties` is ABLATE, and `uncertainties` is REMOVE.

### A.3 `observation.*` (`classifierTypes.ts:398-408`)

| Nested field   | Consumer                                                                                                                                               | Class                     | Verdict                                                                                                  |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------- | -------------------------------------------------------------------------------------------------------- |
| `selector`     | `smwsPolicy.ts:378-379,458,475-476`, `observation.ts:11`                                                                                               | decision-affecting (SMWS) | **KEEP**                                                                                                 |
| `caskNumber`   | `reviewPolicy.ts:1985` (`getExactCaskCodeAnchor` — deterministic exact-cask anchor), `smwsPolicy.ts:97`, `reviewPolicy.ts:1644/1787/1796` (name dedup) | decision-affecting        | **KEEP**                                                                                                 |
| `barrelNumber` | `reviewPolicy.ts:1645/1788/1797` (anchor/name dedup)                                                                                                   | decision-affecting        | **KEEP**                                                                                                 |
| `bottleNumber` | `smwsPolicy.ts:374` passthrough only; `observation.ts:14` normalize                                                                                    | NO decision read          | **ABLATE** — never read for an action; measure whether removing it changes any decision (expected none). |
| `outturn`      | `smwsPolicy.ts:375/455` passthrough; `observation.ts:15` normalize                                                                                     | NO decision read          | **REMOVE** candidate (ABLATE if display value on proposals is wanted).                                   |
| `market`       | `smwsPolicy.ts:376/456` passthrough; `observation.ts:16` normalize                                                                                     | NO decision read          | **REMOVE** candidate.                                                                                    |
| `exclusive`    | `smwsPolicy.ts:377/457` passthrough; `observation.ts:17` normalize                                                                                     | NO decision read          | **REMOVE** candidate.                                                                                    |

`observation.caskNumber`/`barrelNumber`/`selector` earn their place via the exact-cask
anchor and SMWS paths. `outturn`/`market`/`exclusive`/`bottleNumber` are only
normalized and copied; no code branches on them.

### A.4 `confidenceBasis.*` (`classifierTypes.ts:453-495`)

| Nested field       | Consumer                                                                                                                                                   | Class                  | Verdict                                                                                                                                                                                                                |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `band`             | `photo-identification.ts:65-70` (auto-create gate), `reviewPolicy.ts:2603/2626/2656` (auto_verification caps), `classifierRuntime.ts:238`                  | decision-affecting     | **REMOVE** with numeric confidence (`design.md:125,188`); reaffirmation moves to `positiveEvidence`. (Established in `output-contract-map.md:55`.)                                                                     |
| `positiveEvidence` | `photo-identification.ts:312` (`.length` telemetry only), `classifierRuntime.ts:239` passthrough                                                           | telemetry-only         | **ABLATE** — not decision-affecting today. Retype to `{kind,locator,claim}` (`output-contract-map.md:226-251`) and add the citation-exists validator; keep only if the validator rejects fabricated evidence in evals. |
| `unresolvedRisks`  | `reviewPolicy.ts:2627` (`unresolvedRisks.length === 0` gates auto_verification cap), `photo-identification.ts:314` telemetry                               | **decision-affecting** | **KEEP** — the model's veto channel (`spec.md:184-188`).                                                                                                                                                               |
| `toolsUsed`        | `photo-identification.ts:316` telemetry, `classifierRuntime.ts:241` filter/normalize                                                                       | telemetry-only         | **ABLATE** — no decision reads it. Experiment: drop it and confirm no tier changes; keep only as an eval-observability aid if evals show it flags mis-runs.                                                            |
| `webEvidence`      | `priceMatchingProposals.ts:2095` → `getStorePriceMatchAutomationAssessment` (derived tier), `priceMatchingEvidence.ts:754,791,839` (`webEvidenceJudgment`) | **decision-affecting** | **KEEP**                                                                                                                                                                                                               |

---

## B. Input fields (build on `input-envelope-and-terminology.md`)

The input envelope's primary consumer is the **model** (it reads the serialized JSON,
`agentInput.ts:42-70`). Several fields also have **code** consumers off the hydrated
objects. Value assessment for the fields the task named:

| Input field                      | Code consumer(s)                                                                                                                                                                                                                            | Model value                                                                      | Verdict                                                                                                                                                                                                         |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `candidate.score`                | **code**: candidate sort tiebreaker `classifierRuntime.ts:359,367`, `smwsPolicy.ts:144`                                                                                                                                                     | Model-facing value unproven; spec forbids rank-driven matches (`spec.md:74-76`). | **KEEP field for ordering, REMOVE from serialized model envelope.** Confirms `input-envelope...md:89` conditional REMOVE — the code needs `score` to order candidates, but the model should not see the number. |
| `candidate.source`               | **code, decision-affecting**: `source.includes("exact")` → `hasExactAliasMatch` (`classifierRuntime.ts:884`), preload gate (`:492`), SMWS sort (`smwsPolicy.ts:140`), exact/text/repair_parent anchor detection (`reviewPolicy.ts:890-892`) | Redundant with derived `hasExactAliasMatch`.                                     | **KEEP field for code; REMOVE from serialized model envelope** (keep `hasExactAliasMatch` as the model-facing signal). Matches `input-envelope...md:90`.                                                        |
| `candidate.alias`                | **code, decision-affecting**: `priceMatchingEvidence.ts:144,503,518`, `reviewPolicy.ts:788,813,1618,2063-2077` (legacy-name signals, name variants)                                                                                         | Evidence the model interprets.                                                   | **KEEP** (needs one-line prompt semantics, `input-envelope...md:85`).                                                                                                                                           |
| `currentBottle`                  | **code**: reaffirmation/repair via `reference.currentBottleId` in `priceMatchingEvidence.ts:112-127`, `bottleReferenceResolution.ts`; merged into candidates `classifierRuntime.ts`                                                         | Distinguishes the already-assigned record.                                       | **KEEP** (needs semantics, `input-envelope...md:58`).                                                                                                                                                           |
| `imageEvidence.photoSuitability` | **code, decision-affecting**: `reviewPolicy.ts:1760-1762` (blocks deterministic image match when not single-bottle / not readable), `photo-identification-create.ts:117`, `photo-identification.ts:453-459`                                 | Reliability signal.                                                              | **KEEP** (needs sub-object semantics, `input-envelope...md:70`).                                                                                                                                                |
| `imageEvidence.conflicts`        | **code, decision-affecting**: `reviewPolicy.ts:1762` (`conflicts.length > 0` blocks auto image match)                                                                                                                                       | Disagreement signal.                                                             | **KEEP**.                                                                                                                                                                                                       |
| `imageEvidence.fieldCandidates`  | **code**: `reviewPolicy.ts:1778-1786` (deterministic image identity match), `photo-identification.ts:346-357`                                                                                                                               | Per-field OCR guesses.                                                           | **KEEP**.                                                                                                                                                                                                       |
| `imageEvidence.textRegions`      | **none** — grep across `apps/` + `packages/` (excluding dist/tests) returns no reader; nested inside `extractors[]` and shipped but never consumed                                                                                          | none proven                                                                      | **REMOVE from serialized envelope** (or stop serializing the nested extractor text-region detail) — pure token cost.                                                                                            |

---

## C. Tools

Recordings live only for the two **web-search** tools:
`.vitest-evals/recordings/firecrawl_web_search/` (205 JSON) and
`.../openai_web_search/` (205 JSON) — one per fixture per provider. `search_entities`
and `search_bottles` are **not** recorded (`grep -rl` returns 0), because they run
against the mocked local `dataSource`, not the replayed web layer. So recordings prove
web search is exercised on every fixture but give no direct call-frequency for the two
local tools.

| Tool                  | Attachment / frequency                                                                                                                                                                                                                                                                                             | Downstream consumption of results                                                                                                                                                                                                                                            | Verdict                                                                                                                                                                                                                                                                                      |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| web search (provider) | Exactly one attaches in `open` mode; **firecrawl is the configured default** when `FIRECRAWL_API_KEY` is set (`service.ts:188`, `classifierRuntime.ts:928-929` `useFirecrawlWebSearch = allowCandidateExpansion && !!firecrawlApiKey`), else `openai_web_search` (`:974-985`). Neither attaches in `initial_only`. | `searchEvidence` consumed by derived tier via `priceMatchingEvidence.ts` (`evaluateExistingMatchWebEvidence`, `hasSupportiveWebEvidenceForExistingMatch`) and `webEvidence` judgment.                                                                                        | **KEEP** — heavily exercised, decision-affecting.                                                                                                                                                                                                                                            |
| `search_entities`     | Attaches only when `dataSource.searchEntities` **and** `open` mode (`classifierRuntime.ts:937,950-961`). Live call frequency unknown (not recorded), and `collectInitialResolvedEntities` (`:527-570`) already **preloads** brand/bottler/distiller entities before the model runs.                                | **Consumed downstream**: `resolvedEntities` → `reviewPolicy.ts:2884-2885` map → `sanitizeProposedBottleDraft`/`sanitizeResolvedEntityChoice` (`:2801-2828`) nulls any proposed brand/distiller/bottler id not present+type-matched in the resolved set (anti-hallucination). | **ABLATE** — the _results_ have a real consumer, but the **live tool** may be redundant with the preload. Experiment: run create/repair evals with `search_entities` detached (preload only) and compare proposal entity-id fill-rate/correctness; drop the live tool if preload matches it. |
| `search_bottles`      | Candidate-expansion tool, `open` mode only (`classifierRuntime.ts:940-987`); also re-run inside no-match investigation (`:702-706`). Frequency not recorded.                                                                                                                                                       | Expanded candidates feed the same candidate consumers as the seed list.                                                                                                                                                                                                      | **ABLATE (frequency)** — instrument live call counts in an eval run; if the model rarely expands beyond the seed candidates, narrow or drop it.                                                                                                                                              |

---

## D. Runtime stages

None of the three stages has an **eval A/B** proving outcome improvement; each has
**unit tests asserting the mechanism fires**, which is not the same as measured
decision value (`spec.md:208-211`). All three are therefore **ABLATE**.

| Stage                                                                                                                              | Evidence that exists                                                                                                                                                                                                                   | Verdict + experiment                                                                                                                                                                                                                                                            |
| ---------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| no-match web-investigation retry — `shouldRetryNoMatchWithWebInvestigation` (`classifierRuntime.ts:627-650`), applied `:1396-1429` | Unit tests only: negative case `classifier.test.ts:2074` ("does not retry ... known review domain"), image-preservation `:2177`, hint assertion `:2160`. No eval measures recovered decisions.                                         | **ABLATE** — run the fixture eval suite with the retry disabled; measure how many production-miss `no_match` fixtures flip to correct `match`/`create` (recovered) vs regress. Keep only if net-positive without adding false positives (`spec.md:286-288` two-direction rule). |
| preloaded web investigation — `shouldPreloadWebInvestigation` (`classifierRuntime.ts:475-501`), applied `:1359-1386`               | Unit test only: `classifier.test.ts:1918` ("preloads web evidence for whisky-like references without exact local candidates"). Mechanism, not outcome.                                                                                 | **ABLATE** — eval with preload off; measure create-support quality (supported-create rate, false-create rate) and the latency/token cost saved. Keep if create quality drops materially without it.                                                                             |
| entity preresolution — `collectInitialResolvedEntities` (`classifierRuntime.ts:527-570`), applied `:1345-1352`                     | Unit test: `classifier.test.ts:1380-1445` (asserts `searchEntities` called for brand/bottler/distiller and merged). Downstream reader proven (`reviewPolicy.ts:2801-2828`). Whether it improves proposal id-correctness is unmeasured. | **ABLATE** — eval create/repair proposals with preresolution off; measure proposed brand/distiller/bottler id fill-rate and post-`sanitize` survival. Keep if id-correctness drops. (Overlaps the `search_entities` ablation in C — run them together.)                         |

---

## Verdict tally

- **Output fields:** KEEP 10 top-level (+`unresolvedRisks`, `webEvidence`, `observation.selector/caskNumber/barrelNumber`); REMOVE 1 (`confidence`) + `band` + `identityBasis.uncertainties` + (envelope-serialization) `market`/`exclusive`/`outturn`; ABLATE `identityBasis.{bottleTraits,releaseTraits,observationTraits,yearInterpretation,siblingEvidence}`, `observation.bottleNumber`, `confidenceBasis.{positiveEvidence,toolsUsed}`, `aliasScope`; KEEP-but-demote `rationale`.
- **Input fields:** KEEP `alias`, `currentBottle`, `imageEvidence.{photoSuitability,conflicts,fieldCandidates}`; REMOVE-from-model-envelope `candidate.score`, `candidate.source`, `imageEvidence.textRegions` (score/source retained as code-only).
- **Tools:** KEEP web search (firecrawl default); ABLATE `search_entities` (live vs preload) and `search_bottles` (frequency).
- **Runtime stages:** ABLATE all 3 (unit-tested mechanism, no eval outcome proof).
  </content>
